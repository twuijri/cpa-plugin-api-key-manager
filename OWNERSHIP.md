# Ownership

Original project code is authored for Abdulaziz. No public distribution license has
been selected by the owner. Do not publish the repository or submit it to a plugin
store without the owner's direction.

This is an independent implementation, not a fork of CPA Credit Manager or CPA
Key Policy. It implements the public CLIProxyAPI plugin C ABI and JSON contracts.
The host, Go runtime, operating system libraries, and development tools retain
their own respective licenses. This notice does not claim ownership of those.
